# Getting started

## How to start

First thing you need to do is to start the docker container.
Run the following command in the root directory of the project:

```
    docker compose -v up -d
```

to stop the docker contaner do:

```
    docker compose down
```

you might have to delete the pre-existing volumes if you are encountering 